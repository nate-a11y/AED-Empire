{% doc %}
  @name Collections List
  @description Displays all collections in a responsive grid with consistent card styling.
  Images are displayed with a fixed aspect ratio for uniform appearance.
{% enddoc %}

<style>
/* Collections List Page - Scoped Styles */
.collections-list-section {
  padding: var(--spacing-sections, 80px) 0;
}

.collections-list-section .section-header h1 {
  text-align: center;
  margin-bottom: 10px;
}

/* Grid Layout */
.collections-list-section .collections-grid {
  display: grid !important;
  grid-template-columns: repeat(3, 1fr) !important;
  gap: 30px !important;
}

@media (max-width: 1024px) {
  .collections-list-section .collections-grid {
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 24px !important;
  }
}

@media (max-width: 600px) {
  .collections-list-section .collections-grid {
    grid-template-columns: 1fr !important;
    gap: 20px !important;
  }
}

/* Collection Card Base */
.collections-list-section .collection-card {
  display: block !important;
  text-decoration: none !important;
  color: inherit;
  background: var(--color-bg, #fff);
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.collections-list-section .collection-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
}

.collections-list-section .collection-card:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

/* CRITICAL: Image Container - Forces Square Aspect Ratio */
.collections-list-section .collection-card__image {
  position: relative !important;
  width: 100% !important;
  padding-top: 100% !important; /* 1:1 aspect ratio fallback */
  aspect-ratio: 1 / 1 !important;
  overflow: hidden !important;
  background: var(--color-bg-alt, #f5f5f5);
}

/* CRITICAL: Image Styling - Forces Uniform Display */
.collections-list-section .collection-card__image img {
  position: absolute !important;
  top: 0 !important;
  left: 0 !important;
  width: 100% !important;
  height: 100% !important;
  object-fit: cover !important;
  object-position: center !important;
  transition: transform 0.4s ease;
}

.collections-list-section .collection-card:hover .collection-card__image img {
  transform: scale(1.05);
}

/* Placeholder */
.collections-list-section .collection-card__placeholder {
  position: absolute !important;
  top: 0 !important;
  left: 0 !important;
  width: 100% !important;
  height: 100% !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  background: var(--color-bg-alt, #f5f5f5);
  color: var(--color-text-light, #999);
}

/* Hover Overlay */
.collections-list-section .collection-card__overlay {
  position: absolute !important;
  top: 0 !important;
  left: 0 !important;
  right: 0 !important;
  bottom: 0 !important;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.6) 0%, transparent 50%);
  display: flex !important;
  align-items: flex-end !important;
  justify-content: center !important;
  padding-bottom: 20px;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.collections-list-section .collection-card:hover .collection-card__overlay {
  opacity: 1;
}

/* CTA Button */
.collections-list-section .collection-card__cta {
  display: inline-block;
  padding: 10px 24px;
  background: var(--color-primary, #C93C3C);
  color: #fff !important;
  font-size: 0.875rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  border-radius: 4px;
  transform: translateY(10px);
  transition: transform 0.3s ease;
}

.collections-list-section .collection-card:hover .collection-card__cta {
  transform: translateY(0);
}

/* Card Content */
.collections-list-section .collection-card__content {
  padding: 20px;
  text-align: center;
}

.collections-list-section .collection-card__title {
  margin: 0 0 4px !important;
  font-size: 1.125rem !important;
  font-weight: 600 !important;
  color: var(--color-text, #1a1a2e) !important;
  line-height: 1.3;
}

.collections-list-section .collection-card__count {
  margin: 0 !important;
  font-size: 0.875rem;
  color: var(--color-primary, #C93C3C);
}

/* Reduced Motion */
@media (prefers-reduced-motion: reduce) {
  .collections-list-section .collection-card,
  .collections-list-section .collection-card__image img,
  .collections-list-section .collection-card__overlay,
  .collections-list-section .collection-card__cta {
    transition: none !important;
  }
  .collections-list-section .collection-card:hover .collection-card__image img {
    transform: none !important;
  }
}
</style>

<section class="section collections-list-section">
  <div class="container">
    {%- if section.settings.title != blank -%}
      <div class="section-header">
        <h1>{{ section.settings.title }}</h1>
        {%- if section.settings.subtitle != blank -%}
          <p>{{ section.settings.subtitle }}</p>
        {%- endif -%}
        <div class="section-header__divider" aria-hidden="true"></div>
      </div>
    {%- else -%}
      <h1 class="visually-hidden">Collections</h1>
    {%- endif -%}

    <div class="collections-grid">
      {%- liquid
        assign collections_to_show = collections | where: 'products_count', '>', 0
        for collection in collections
          unless collection.handle == 'all' or collection.products_count == 0
            assign has_image = false
            assign image_url = ''
            assign image_alt = collection.title | escape

            if collection.image
              assign has_image = true
              assign image_url = collection.image | image_url: width: 800
            elsif collection.products.first.featured_image
              assign has_image = true
              assign image_url = collection.products.first.featured_image | image_url: width: 800
            endif
      -%}
        <a href="{{ collection.url }}" class="collection-card" aria-label="View {{ collection.title }} collection">
          <div class="collection-card__image">
            {%- if has_image -%}
              <img
                src="{{ image_url }}"
                alt="{{ image_alt }}"
                width="400"
                height="400"
                loading="lazy"
              >
            {%- else -%}
              <div class="collection-card__placeholder">
                <svg aria-hidden="true" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <rect x="3" y="3" width="18" height="18" rx="2"/>
                  <circle cx="8.5" cy="8.5" r="1.5"/>
                  <path d="M21 15l-5-5L5 21"/>
                </svg>
              </div>
            {%- endif -%}
            <div class="collection-card__overlay">
              <span class="collection-card__cta">Shop Now</span>
            </div>
          </div>
          <div class="collection-card__content">
            <h2 class="collection-card__title">{{ collection.title }}</h2>
            <p class="collection-card__count">{{ collection.products_count }} {{ collection.products_count | pluralize: 'product', 'products' }}</p>
          </div>
        </a>
      {%-
          endunless
        endfor
      -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Collections list",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Our Collections"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "_if": "title"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 2,
      "max": 4,
      "step": 1,
      "label": "Columns on desktop",
      "default": 3
    },
    {
      "type": "checkbox",
      "id": "show_product_count",
      "label": "Show product count",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Collections list",
      "category": "Collection"
    }
  ]
}
{% endschema %}
