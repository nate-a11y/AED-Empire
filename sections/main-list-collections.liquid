{% doc %}
  @name Collections List
  @description Displays all collections in a responsive grid with consistent card styling.
  Images are displayed with a fixed aspect ratio for uniform appearance.
{% enddoc %}

{%- comment -%} Styles are in assets/theme.css under "COLLECTIONS LIST PAGE" section {%- endcomment -%}

<section class="section collections-list-section">
  <div class="container">
    {%- if section.settings.title != blank -%}
      <div class="section-header">
        <h1>{{ section.settings.title }}</h1>
        {%- if section.settings.subtitle != blank -%}
          <p>{{ section.settings.subtitle }}</p>
        {%- endif -%}
        <div class="section-header__divider" aria-hidden="true"></div>
      </div>
    {%- else -%}
      <h1 class="visually-hidden">Collections</h1>
    {%- endif -%}

    <div class="collections-grid">
      {%- liquid
        assign collections_to_show = collections | where: 'products_count', '>', 0
        for collection in collections
          unless collection.handle == 'all' or collection.products_count == 0
            assign has_image = false
            assign image_url = ''
            assign image_alt = collection.title | escape

            if collection.image
              assign has_image = true
              assign image_url = collection.image | image_url: width: 800
            elsif collection.products.first.featured_image
              assign has_image = true
              assign image_url = collection.products.first.featured_image | image_url: width: 800
            endif
      -%}
        <a href="{{ collection.url }}" class="collection-card" aria-label="View {{ collection.title }} collection">
          <div class="collection-card__image">
            {%- if has_image -%}
              <img
                src="{{ image_url }}"
                alt="{{ image_alt }}"
                width="400"
                height="400"
                loading="lazy"
              >
            {%- else -%}
              <div class="collection-card__placeholder">
                <svg aria-hidden="true" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <rect x="3" y="3" width="18" height="18" rx="2"/>
                  <circle cx="8.5" cy="8.5" r="1.5"/>
                  <path d="M21 15l-5-5L5 21"/>
                </svg>
              </div>
            {%- endif -%}
            <div class="collection-card__overlay">
              <span class="collection-card__cta">Shop Now</span>
            </div>
          </div>
          <div class="collection-card__content">
            <h2 class="collection-card__title">{{ collection.title }}</h2>
            <p class="collection-card__count">{{ collection.products_count }} {{ collection.products_count | pluralize: 'product', 'products' }}</p>
          </div>
        </a>
      {%-
          endunless
        endfor
      -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Collections list",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Our Collections"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "_if": "title"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 2,
      "max": 4,
      "step": 1,
      "label": "Columns on desktop",
      "default": 3
    },
    {
      "type": "checkbox",
      "id": "show_product_count",
      "label": "Show product count",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Collections list",
      "category": "Collection"
    }
  ]
}
{% endschema %}
